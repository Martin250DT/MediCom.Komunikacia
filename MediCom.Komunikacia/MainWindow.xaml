<Window x:Class="MediCom.Komunikacia.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:MediCom.Komunikacia.Converters"
        mc:Ignorable="d"
        Title="MediCom ChatGPT" Height="720" Width="1180" MinHeight="540" MinWidth="920"
        Background="{StaticResource BackgroundBrush}">
    <Window.Resources>
        <conv:BooleanToHorizontalAlignmentConverter x:Key="AlignmentConverter" />
        <conv:BooleanToBrushConverter x:Key="BubbleBrushConverter"
                                      UserBrush="{StaticResource OutgoingBubbleBrush}"
                                      AssistantBrush="{StaticResource IncomingBubbleBrush}" />
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Background="{StaticResource SidebarBrush}" Grid.Column="0">
            <DockPanel>
                <Border Background="{StaticResource HeaderBrush}" Padding="18">
                    <StackPanel>
                        <TextBlock Text="MediCom Chat" FontSize="24" FontWeight="SemiBold" Foreground="White" />
                        <TextBlock Text="ChatGPT vo WPF • .NET Framework 4.8" Margin="0,6,0,0" Foreground="#FFB7C8D4" />
                    </StackPanel>
                </Border>

                <StackPanel Margin="18">
                    <Border CornerRadius="14" Background="#FF202C33" Padding="16" Margin="0,18,0,12">
                        <StackPanel>
                            <TextBlock Text="Aktívny kontakt" Foreground="#FF8696A0" FontSize="12" />
                            <TextBlock Text="ChatGPT Assistant" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,4,0,0" />
                            <TextBlock Text="Online cez OpenAI API" Foreground="#FF25D366" Margin="0,8,0,0" />
                        </StackPanel>
                    </Border>

                    <Border CornerRadius="14" Background="#FF202C33" Padding="16">
                        <StackPanel>
                            <TextBlock Text="Použitie" Foreground="#FF8696A0" FontSize="12" />
                            <TextBlock TextWrapping="Wrap" Foreground="White" Margin="0,8,0,0"
                                       Text="1. Doplň OpenAiApiKey do App.config&#x0a;2. Spusť aplikáciu vo Visual Studiu&#x0a;3. Píš správy dole do inputu" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource HeaderBrush}" Padding="18,14">
                <DockPanel LastChildFill="False">
                    <StackPanel DockPanel.Dock="Left">
                        <TextBlock Text="ChatGPT Assistant" Foreground="White" FontSize="20" FontWeight="SemiBold" />
                        <TextBlock Text="Inteligentný pomocník v štýle WhatsApp" Foreground="#FFD1D7DB" Margin="0,4,0,0" />
                    </StackPanel>
                    <Border DockPanel.Dock="Right" Background="#FF25D366" CornerRadius="12" Padding="12,6">
                        <TextBlock Text="Online" Foreground="#FF111B21" FontWeight="Bold" />
                    </Border>
                </DockPanel>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="22">
                <ItemsControl ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border HorizontalAlignment="{Binding IsUser, Converter={StaticResource AlignmentConverter}}"
                                    Background="{Binding IsUser, Converter={StaticResource BubbleBrushConverter}}"
                                    CornerRadius="16"
                                    MaxWidth="480"
                                    Padding="14"
                                    Margin="0,0,0,14">
                                <StackPanel>
                                    <TextBlock Text="{Binding Sender}" FontWeight="SemiBold" Margin="0,0,0,6" />
                                    <TextBlock Text="{Binding Text}" TextWrapping="Wrap" FontSize="15" />
                                    <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:HH:mm}'}"
                                               HorizontalAlignment="Right"
                                               FontSize="11"
                                               Foreground="#FF667781"
                                               Margin="0,8,0,0" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <Border Grid.Row="2" Background="#FFF0F2F5" Padding="18">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0"
                             Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             MinHeight="58"
                             MaxHeight="120"
                             VerticalContentAlignment="Center"
                             Padding="16,14"
                             FontSize="15"
                             BorderThickness="0"
                             Background="White"
                             Margin="0,0,12,0" />
                    <Button Grid.Column="1"
                            Content="Odoslať"
                            Command="{Binding SendMessageCommand}"
                            Padding="24,14"
                            MinWidth="130"
                            Background="#FF25D366"
                            Foreground="#FF111B21"
                            FontWeight="SemiBold"
                            BorderThickness="0" />
                </Grid>
            </Border>

            <Border Grid.Row="2" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="24,-30,0,0"
                    Background="#CC202C33" CornerRadius="10" Padding="12,6">
                <TextBlock Text="{Binding StatusText}" Foreground="White" FontSize="12" />
            </Border>
        </Grid>
    </Grid>
</Window>
